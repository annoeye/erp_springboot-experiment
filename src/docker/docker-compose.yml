include:
  - compose/oracle-db.yml
  - compose/redis.yml
  - compose/kafka.yml
  - compose/minio.yml
  - compose/spring-app.yml

volumes:
  oracle-data:
  redis-data:
  kafka-data:
  minio-data:

networks:
  app-network:
    driver: bridge